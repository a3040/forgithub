{
    "configurations": [
        {
            "command": "npm start",
            "name": "start express1",
            "cwd": "${workspaceFolder}/express1",
            "request": "launch",
            "type": "node-terminal" 
        }
    ]
}